Trabajador is a career organizer that helps users manage their courses and tasks. It allows users to add filters, monitor progress, and stay organized as they work through their professional goals.

Features:

Organize courses and tasks.
Progress tracking and filtering options.
Manage career growth efficiently.
Technologies:

Backend: PHP, Laravel, Livewire
Frontend: Tailwind CSS, Alpine.js
Setup Instructions:

Clone the repository: git clone [repository URL]
Install dependencies: composer install && npm install
Run database migrations: php artisan migrate
Serve the application: php artisan serve
